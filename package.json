{
  "name": "3cx-health-job",
  "version": "0.0.1",
  "description": "Check 3cx health and report to healthchecks.io",
  "main": "src/main.ts",
  "scripts": {
    "build": "tsc && cp package.json dist/ && cp package-lock.json dist/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^16.11.7",
    "typescript": "^4.5.2"
  },
  "dependencies": {
    "@3cx/api": "^0.0.36",
    "axios": "^0.24.0"
  }
}
